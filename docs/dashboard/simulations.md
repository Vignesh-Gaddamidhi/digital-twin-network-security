# Simulation Control Center
- Five lifecycle actions: Start, Pause, Resume, Stop, Reset.
- 13 security attack scenarios and micro-patterns.
- Multi-stage timeline: `NORMAL` $\to$ `EARLY_INDICATORS` $\to$ `ESCALATION` $\to$ `IMPACT` $\to$ `RECOVERY`.
- Deterministic seed configuration for reproducibility.