# Phase 6: Simulation Event Flow
SIMULATION ENGINE -> SimulationEvent -> State Engine -> Twin State (Simulation) -> Effective State

- Accepts synthetic failure events, traffic spikes, and exploitation maneuvers.
- Applies conflict precedence overrides to calculate the `effective` state.