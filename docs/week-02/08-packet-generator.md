# Day 12: Generator Configuration Specification

Profiles are controlled via a JSON configuration manifest defining:
- `endpoints`: Network node identities matching our Digital Twin canonical inventory (D001–D005).
- `profiles`: Enabled traffic generation behaviors.
- `timing`: Packet interval, loop counts, and inter-session delays.